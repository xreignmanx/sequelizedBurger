<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Eat Me!{{else}}Available!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
